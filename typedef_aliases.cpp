#include <iostream>
#include <vector>

typedef std::vector<std::pair<std::string, int>> pairlist_t;

typedef std::string text_t;
typedef int int_t;

using food = std::string;

int main(){
    text_t firstname = "abbi";
    food fastfood = "sandwich";
    std::cout << fastfood << '\n';
    
    return 0;
}
